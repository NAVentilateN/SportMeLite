<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= content_for?(:title) ? yield(:title) : "SportMe | Reimagining Your Fitness Experience" %>
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="<%= meta_description %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag %>

    <!-- Facebook Open Graph data -->
    <meta property="og:title" content="<%= meta_title %>" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:image" content="<%= meta_image %>" />
    <meta property="og:description" content="<%= meta_description %>" />
    <meta property="og:site_name" content="<%= meta_title %>" />
    <link href="https://api.mapbox.com/mapbox-gl-js/v2.6.1/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v2.6.1/mapbox-gl.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= favicon_link_tag 'https://res.cloudinary.com/dcwfy3dua/image/upload/v1642211432/sportmelite/favicon/favicon-32x32_svjtun.png'%>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload'  %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <script src="https://code.iconify.design/1/1.0.4/iconify.min.js"> </script>
    <style>
      <%= ".message-#{current_user&.id}" %> {
      text-align: right;
      }
      <%= ".cont-#{current_user&.id}" %> {
      background: #e8f1f3 !important;
      text-align: right;
      float: right;
      }

    </style>
  </head>
  <body>
    <% unless controller_name == "pages" %>
      <%= render 'shared/navbar' %>
      <%= render 'shared/flashes' %>
      <%= render 'shared/modeswitch' %>
    <% end %>
    <div class='page-container'>
      <%= yield %>
      <%= render 'shared/scrollbutton' %>
      <footer>
        <%= render 'shared/footer' %>
      </footer>
    </div>
  </body>
</html>

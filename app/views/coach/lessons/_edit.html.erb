  <%= simple_form_for(lesson, method: :patch, url: coach_lesson_path, html: { class: lesson.errors.any? ? '' : 'd-none', data: { lesson_target: 'editForm', action: 'submit->edit-lesson#update' } } ) do |f|%>
      <%= f.input :start_date_time, :as => :datetime %>
      <%= f.input :end_date_time, :as => :datetime %>
      <%= f.input :description %>
      <%= f.input :location %>
      <%= f.input :price, as: :numeric %>
      <%= f.submit class:'btn btn-primary' %>
  <% end %>


